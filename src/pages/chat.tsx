import React from "react";
import { Container, Typography } from "@material-ui/core";

const Chat: React.FC = () => {
  return (
    <React.Fragment>
      <Container>
        <Typography>チャット</Typography>
      </Container>
    </React.Fragment>
  );
};

export default Chat;
